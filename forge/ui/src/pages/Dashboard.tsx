import React from 'react';

// Dashboard is rendered inside AppShell's three-column layout.
// The ControlPanel (column 2) and OutputPane (column 3) are
// mounted directly in AppShell when the route is '/'.
// This component exists only as a route target placeholder.
const Dashboard: React.FC = () => {
  return null;
};

export default Dashboard;
